This is the site code for coolingtowers.us.

Run 'rake' to generate the site. The Rakefile converts Haml to HTML, 
Sass to CSS, generates the image gallery, and implements an include feature.

ImageMagick (convert) is required to generate the image thumbnails.

 ,   copyright 2012 pete gamache, pete@gamache.org
(k)  all rights reversed
